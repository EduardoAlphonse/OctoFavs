import React from 'react';
import { View, Text } from 'react-native';

import { styles } from './styles';

export const RemoveConfirmationModal = () => {
	return (
		<View style={styles.container}>
			<Text>RemoveConfirmationModal</Text>
		</View>
	);
};
